// Root build.gradle (Gradle 8.x compatible)

buildscript {
    dependencies {
        classpath "com.android.tools.build:gradle:8.2.0"
    }
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
